const Auth	= require( "./auth" );
const Filesytem	= require( "./filesystem" );

const Misuse = function( config ){
	const self = this;
	async.waterfall( [ function( cb ){
		Validations.validate( config, Validations.
};

module.exports = Misuse;
